# FRC Ball Tracker - Python Dependencies
# Install with: pip install -r requirements.txt
# Note: For CUDA-enabled PyTorch, use the batch script instead

# Core (required)
numpy>=1.21.0
opencv-python>=4.5.0
scipy>=1.7.0

# YOLO detection (required for robot tracking)
ultralytics>=8.0.0

# PyTorch - install separately with CUDA support:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# For older Python (<3.13), cu124 or cu121 also work:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# WARNING: plain "pip install torch" gives CPU-only build - YOLO will be very slow!
# Verify with: python -c "import torch; print(torch.__version__)"  # should show +cu126

# GPU acceleration (optional but recommended)
# Install separately: pip install cupy-cuda12x
# Or for CUDA 11: pip install cupy-cuda11x

# Optional extras
easyocr>=1.6.0
pandas>=1.3.0
matplotlib>=3.4.0
